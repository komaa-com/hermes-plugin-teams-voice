name: tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.12"]
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install package + test deps
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[numpy]" pytest

      # Standalone run (no Hermes host): the few tests that need the Hermes
      # runtime self-skip via pytest.importorskip; the rest exercise the
      # pure-logic + handler wiring.
      - name: Run tests
        run: pytest hermes_teams_voice/tests -q
